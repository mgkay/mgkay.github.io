# PCV Planning Protocol
**Planning** phase. Follow steps in order.
Read `pcv-common.md` first for shared patterns (log formats, dispatch, gates).
Then read `planning/step1-charge-config.md` and follow the step sequence.
