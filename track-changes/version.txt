5.0.0
2026-06-03
track-changes v5: `polish` joins as the THIRD cooperating skill (track-changes + verified-import + polish). `/polish [file]` cleans up VOICE-DICTATED prose — speech-recognition errors, grammar, dropped words — in an existing tracked .md/.qmd document, surfacing every fix as an ordinary track-changes `<mark>` (reviewable via `/tc accept|reject`). Opt-in, default-OFF: the explicit `/polish` invocation IS the opt-in (no marker file). Bright line — it NEVER auto-corrects an unrecognized token (jargon/code/math/domain term): such a token is left + flagged, never silently changed; a meaning-affecting fix is surfaced as a mark, not applied silently. polish adds NO new hook and NO settings change — its fixes flow through the EXISTING track-changes PreToolUse hook, so the suite hook count stays SIX; on a tracked file fixes become marks, on an untracked file it offers a one-time direct polish (no marks) or to enable tracking first. polish_engine.py reuses track-changes' own tc_core.activation for the tracked-check and imports tc_core from track-changes/lib (no duplicate copy — same shared-core pattern as verified-import). No view-time "dictated lens" (an earlier render-time second color was retired after a source-vs-executed token-stream drift; `git diff` is the authoritative "what changed" view). The mark grammar, hooks, and the two prior skills are UNCHANGED; existing tracked documents need NO conversion. The bootstrap version check is corrected to 5.0.0 (the published 4.x bootstrap still referenced 4.0.0 after the 4.1 files shipped); v5 also reconciles the source-of-record up to the published 4.1 content for the first time.

4.1.0
2026-05-31
track-changes v4.1: per-file activation key renamed from `track-changes` to `tc-track` (YAML) / `% tc-track:` (.tex magic comment). The old `track-changes` key is a RESERVED Quarto YAML field (Quarto accepts only accept/reject/all and errors on true/false), so it broke every .qmd/.md render with a YAML-validation error; `tc-track` is an unknown key Quarto ignores. Skill name, the `/tc` command, the mark grammar, the hooks, and `.tc-tracked` folder markers are unchanged. BREAKING for files using the old key — the gate no longer recognizes `track-changes`, so swap those to `tc-track`. Updated gate/reader/writer (tc_core/activation.py, tc-common.sh, tc-cli.sh), the marker template (tc-mark.sh), and all docs (SKILL.md, digest.md, latex.md, bootstrap.md, commands/tc.md, index.html). Also hardened `/draft` so a Skill-tool invocation (which does not run the `!bash` sentinel writer) self-verifies and falls back to running draft-on.sh via Bash.

4.0.0
2026-05-27
track-changes v4: import-verification rethink. verified-import's mechanical content-word gate is removed — a /import now lands clean by default via a one-shot sha-bound exemption that track-changes consumes, and the LLM self-marks only genuinely significant changes (an added or removed sentence or clause, or a changed quantity, term, or formula; reflow, formatting, and equivalent notation are not significant). This fixes cross-format false-rejections — e.g. a faithful LaTeX-to-Markdown import of an equation like Q* = sqrt(2KD/h) — that the v3 token-multiset gate could never accept, because the two formats tokenize the same math differently. The /import CLI now strips a leading @ file-reference prefix and emits UTF-8, so math symbols and em-dashes no longer mangle on a Windows console. The track-changes mark core is unchanged and existing tracked documents need no conversion; the text-source-only contract stays. SC3 (significant changes are surfaced for review) is a behavioral contract — significance is the model's judgment, verified by mechanism tests, with no mechanical backstop. 379/379 tests pass.
