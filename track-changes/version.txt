1.13
2026-05-24
Native Python hooks (validator + audit) with shared daemon; walk-based per-region coverage; resolution-mode pre-pass (AI can accept/reject on instruction); HTML <s> strikethrough encoding. Per-tracked-edit overhead 154ms; 198/198 tests pass.
