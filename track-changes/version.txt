9.13.1
2026-08-04
track-changes v9.13.1 -- the two long-deferred items closed on evidence; the third stays deferred.
- FR-4 CLOSED. No defect exists. Logged as "a clean repro" of a hook bug; both diagnoses are now
  disproved. 9.9.3: the non-inline detector has no HTML handling at all, and "spans a non-inline
  construct" is a HINT appended to a block, never a block reason -- the log conflated the hint
  with the cause. 9.13.1: the follow-up worry is disproved too -- stripping `<https://...>` before
  citation detection does NOT hide a citation, because the footnote brackets sit OUTSIDE the
  stripped span, so the footnote match still fires. Seven shapes measured. Reviving it would need
  the original 2026-08-01 blocked payload, which no longer exists. Pinned by TC-AI-51 so the
  non-bug cannot be "fixed".
- FR-9 CLOSED, resolved by 9.13.0. The invariant asked for the author's tweaks in one commit and
  the AI's marks in another; that was documented-but-impossible while the two were interleaved
  hunks in one file. 9.13.0 made the split fall out of the ORDERING instead: measured per commit,
  the documented flow yields one commit carrying only the author's text and another carrying only
  the AI's. The originally proposed mechanism (write the baseline, commit, write the current
  content, commit) is now unnecessary AND worse -- it would write stale content into the user's
  file to manufacture a commit. Pinned by TC-AI-52, which classifies each commit by what its diff
  ADDS rather than counting commits.
- RESIDUAL, documented rather than engineered: asking for corrections WITHOUT running /tc edits
  first puts both authorships in one commit. Honest, but the attribution is gone and cannot be
  recovered. reference/tc-edits.md now tells the model to run /tc edits first.
- BODY-HASH TRIGGER: still deferred, WITH ITS REVISIT CONDITION INTACT. 9.12.0 deferred it on the
  rule "escalate if the accept-time NOTE fires with any regularity in real use." 9.12.0 was
  installed today; the NOTE has fired zero times outside tests. Building it now would be exactly
  the guess-about-frequency the deferral exists to avoid, and would add the refusal path 9.12.0
  deliberately declined. Not an omission -- the correct disposition.
- No behaviour change. One shipped doc line, two tests. No new hook (6), no migration.
- 872-test suite (+4 Category AI: 88); 0 regressions.

9.13.0
2026-08-04
track-changes v9.13.0 -- the commit is the tool's job, not yours.
- BEHAVIOUR-CHANGING: track-changes now CREATES GIT COMMITS in your repository. Read the next
  three entries before upgrading.
- THE FINDING: 8.1.0 requires resolution to run on COMMITTED CONTENT. It never required the
  HUMAN to be the one who commits. Those were conflated, and the cost was TWO manual commits per
  review round -- at the two moments you are trying to finish. `/tc edits` refused until you
  committed; then the AI's marks made the file dirty again and `/tc accept` refused too.
- NOW: `/tc edits <file>` commits your edits ("Instructor edits: <file>") and accepts the
  regions you edited -- you reviewed them by editing them -- then commits that too, separately.
  `/tc accept`/`/tc reject` likewise commit the file instead of refusing. One command after you
  finish editing; you re-engage only when Claude's corrections arrive as marks.
- ONLY THE NAMED FILE IS EVER COMMITTED. `git commit -- <path>`, never `-a`, never bare. Other
  dirty files, and other STAGED files, are left exactly as they were, and the report says so.
  TC-AI-45 plants both and asserts they survive; `git commit -a` passes every other test.
- NEVER `--no-verify`: a pre-commit hook that refuses is a commit FAILURE, it is reported, and
  NOTHING is resolved afterwards.
- THE LINE THAT DOES NOT MOVE: AI corrections are never auto-accepted. The round stops and hands
  them to you. Untouched regions and pending inline marks stay pending -- they are AI proposals
  you have not read, and approving text unread is the one thing the invariant forbids.
- ACCEPTED REDUCTION: the old gate prescribed commit -> REVIEW THE DIFF -> re-run. The middle
  step is now skipped. You review when you edit and again when you rule on the marks, and the
  diff stays in history.
- `TC_NO_AUTOCOMMIT=1` restores the old refusal in full.
- A bare `/tc edits` across SEVERAL changed files reports only and commits nothing -- resolving a
  whole working set from one unqualified command is the over-reach the scoping exists to prevent.
- Also: `resolve` finally removed from the tc-cli dispatch list, where it had survived 10.0.0's
  withdrawal as a dead branch reaching an argparse that rejects it.
- No grammar change, no new hook (6), no migration.
- 868-test suite (+25 Category AI: 84); 0 regressions. These are the FIRST tests of the
  committed-content gate in the project's history.

9.12.0
2026-08-04
track-changes v9.12.0 -- an AI edit inside a region body is now recorded, and named at accept.
- THE HOLE: everything the AI writes INSIDE a `.tc-region` body is UNMARKED. Region lines are
  covered by the region's own number, so no per-change mark is required -- measured against the
  real gate: rewriting a body line, adding a line, and replacing the whole body were all allowed
  with no mark (only deletion is separately gated), while the same write to prose OUTSIDE the
  region was correctly refused.
- That is RIGHT for a region the AI wrote that nobody has touched: it is one unit pending review,
  and inline marks do not belong inside it. It stops being right the moment the AUTHOR edits the
  body -- the workflow 9.11.2 documents. From then on the region holds both authorships, the v6
  guarantee ("everything the AI writes is tracked") does not hold inside it, and `/tc accept N`
  absorbs any AI tidying into the author's own prose with nothing to show it.
- NOW RECORDED: every AI write that changes a region body appends a `region-body:` entry to
  `.tc-history.md`, naming the region and the added/removed counts. Unconditional -- no trigger,
  no snapshot dependency.
- NOW NAMED AT THE DECIDING MOMENT: `/tc accept N` prints a NOTE when the log shows that region's
  body was modified after it was created, because accept is what makes the change unrecoverable
  as information. ADVISORY: exit code unchanged, never a refusal.
- WHY NOT A REFUSAL: it would break the author who wants the AI to keep working on a still-pending
  region they have edited (accept early, or /draft, both worse), and a hard guarantee resting on
  the best-effort snapshot trigger reads as a guarantee it is not. Marks inside a region were
  rejected too: they contradict the grammar and force nested resolution. See charge-9.12.0.md.
- HONEST LIMIT: this does NOT prevent the absorption. Same standing as 9.1.0's citation scanner
  and 9.10.0's provenance check -- a conservative flag with the author as backstop. Absence of a
  record means NOT RECORDED, never "clean": a region predating 9.12.0 has no entries.
- DEFERRED, recorded with its revisit condition: a body-hash trigger (hash each region body as the
  gate writes it, so a later write can tell the body on disk is not what the AI last wrote) would
  be sound enough to carry a real refusal. Escalate if the accept-time warning fires with any
  regularity in real use.
- Detection is in PreToolUse (the only place with the true pre-image), recording in PostToolUse.
  Diffing snapshot generations would be less code and WRONG -- that diff is the author's edits plus
  the AI's, inseparably, so it would attribute the author's own region edits to the AI. TC-AI-41
  pins this.
- Additive: no new module, no new hook (6), no grammar change, no migration.
- 843-test suite (+16 Category AI: 59); 0 regressions.

9.11.2
2026-08-04
track-changes v9.11.2 -- documenting how you review a REGION, and un-shipping a withdrawn command.
- THE MISSING POINT, now documented in both places a reader looks: a region is atomic, but its
  BODY IS EDITABLE BEFORE RESOLUTION. Edit it, commit, then `/tc accept N` -- which keeps the
  body AS THE AUTHOR LEFT IT. `accept` never inspected who wrote the body. Deleting part of a
  region IS how that part is rejected, which is why there is no partial-reject command. The
  unedited remainder is approved by having read the region looking for the parts that weren't.
  This was the conclusion the 10.0.0 withdrawal turned on and it appeared in no user-facing
  document -- only in the version chain and a build record.
- VERIFIED, not recalled: a 4-line region, one line rewritten and one deleted by hand,
  committed, `/tc accept 1` -- wrapper gone, rewrite kept, deletion honoured, untouched lines
  kept, neighbouring prose intact. Promoted into the suite as TC-AI-36, because the landing
  page now promises this to readers.
- ORDER MATTERS, and it is now stated: polish AFTER the accept, never before. Measured against
  the real gate, an AI edit INSIDE a region body needs no mark and lands invisibly -- every line
  is already covered by the region's own number (editing a line, adding a line and rewriting the
  whole body were all allowed unmarked; only deletion is separately gated). Once the wrapper is
  gone the ordinary rule applies again and an unmarked change is refused.
- FIXED: `reference/tc-edits.md` documented `/tc edits resolve` -- WITHDRAWN before 10.0.0 ever
  deployed -- for four releases, in the file the MODEL reads. Shipped instructions for something
  argparse rejects. TC-AI-19 could not see it because its derived check only ever opened
  README.md and GUIDE.md; it now globs the whole shipped doc surface (SKILL.md, reference/*.md,
  commands/*.md), which is the general fix rather than naming the file that happened to be wrong.
- The `#ex-edits` walkthrough now COMPLETES -- through commit and accept. It previously ended
  with two unresolved highlights and no next step, and orientation for the report block moved
  ahead of it while the snapshot-vs-git mechanics moved below the payoff.
- No source change to any behaviour: docs, one test widened, three tests added.
- 827-test suite (+9 Category AI: 43); 0 regressions.

9.11.1
2026-08-04
track-changes v9.11.1 -- the landing page joins the repo; the report stops reassuring.
- REPORT WORDING (`/tc edits`): the `polish:` line is renamed `protected:`, which is what it
  has always reported -- jargon/code/math tokens the model must LEAVE ALONE. Its empty branch
  used to print "nothing flagged in the edited text", meaning *no un-correctable tokens* and
  reading as *no errors found*. Measured on prose carrying a doubled "the" and "weights is"
  for "weights are": it printed exactly that. `protected: none` means nothing needed
  protecting, never that nothing needed fixing.
- DROPPED with it: "run /tc polish for the full editorial pass". In this flow the editorial
  pass is the model reading those spans NOW; the pointer sent the reader away at the moment
  the work should start. The polish-vs-edits distinction lives in reference/tc-edits.md and
  on the landing page, where someone choosing between two commands actually looks.
- NEW closing line: `NOT PROOFREAD: nothing above has been checked for grammar, dropped
  words, or sense.` Every line above it is MECHANICAL. Printed only when there is at least
  one content span -- with none the instruction is noise.
- THE LANDING PAGE IS NOW IN THIS REPO (`pages/`): index.html, style.css, img/. They lived
  only in the Pages repo -- outside this repo, outside PCV, outside the private mirror --
  while publish.sh rewrote the page's footer. "Never hand-edit Pages" could not be honored
  for the one file most likely to need editing.
- publish.sh syncs `pages/` generically, so a new asset needs no edit there. The page is a
  BYTE COPY: the destination-`sed` that rewrote the footer is GONE (it would have made source
  and destination differ permanently, so every publish would re-copy). The footer version now
  lives in the source and is checked twice -- the suite fails on drift, and publish.sh
  refuses.
- publish.sh REPORTS stale Pages files. Its comment had promised this since v9 and no such
  code existed; 9.11.1 made the comment true rather than deleting the claim. Reporting only,
  never deletion -- publish is copy-based by design.
- No grammar, hook (6), or resolution change. No migration.
- 818-test suite (+17 Category AI: 34, +1 AH: 36); 0 regressions.

9.11.0
2026-08-03
track-changes v9.11.0 -- an install stops accumulating.
- BEHAVIOR-CHANGING FOR `install.sh`: it now MOVES files. Until now it only ever copied, so an
  installed tree was the union of every version ever installed over it. Measured on the live
  install right after the 9.10.0 upgrade, five files the source does not ship: hooks/pre-tool-use.sh
  and hooks/post-tool-use.sh (THE v2 ENFORCEMENT AND AUDIT HOOKS), lib/tc_activation.py,
  lib/tc_audit.py, lib/tc_provenance.py. Nothing referenced them -- but tc_selfcheck answered 27
  modules where the source has 26, and that check is the gate both deployment paths use.
- WHAT IT DOES NOW: `copy_one` (the one funnel every shipped file passes through) records what
  this run deployed; after copying, and before the self-check, any other *.py/*.sh under lib/ or
  hooks/ is QUARANTINED to <skill>/.stale/<UTC-timestamp>/<relative path> and NAMED on stdout.
  Nothing is deleted. Remove .stale/ yourself once you have looked at it.
- YOUR DATA IS NOT TOUCHED. state/ (the /tc edits snapshot store, the mark cache) and validation/
  (source snapshots, manifests, dated web captures) are never visited. reference/, tools/ and
  commands/ are left alone in this release -- a stale .md is noise, not a hazard, and every path
  not pruned is a path that cannot be pruned wrongly.
- KEPT REGARDLESS: hooks/pcv_dev_gate.py. `--dev-guards` installs it with a bare cp outside
  copy_one, so it is invisible to the record BY CONSTRUCTION and a naive prune would delete the
  maintainer's own governance hook on the next ordinary install.
- DERIVED, NEVER TYPED. An enumerated prune list would carry the same blind spot as the
  enumerated copy list that caused 9.9.0. The record is a by-product of copying.
- NOTHING IS PRUNED on a failed copy (a partial record must never become a prune list) or under
  --dev-guards (it deploys one file and is not a full install).
- IF YOU KEEP YOUR OWN FILE under a skill's lib/ or hooks/, it will be quarantined. Move it
  elsewhere. The quarantine is recoverable and the report names it.
- THE PAGES BOOTSTRAP STILL DOES NOT PRUNE, and now says so. It downloads an enumerated list with
  no copy-step record, so it cannot tell an obsolete file from one it never knew about. Run
  `bash install.sh` from a source checkout to clean an accumulated tree.
- 9.11.0, not 9.10.1: an upgrade that moves files is behavior users must be told about.
- No source, grammar, or resolution change. Hook count stays 6. No document migration.
- 803-test suite (+11 Category AI: 20); 0 regressions.

9.10.0
2026-08-03
track-changes v9.10.0 -- accept-time provenance check; the Phase B `resolve` command withdrawn.
- MINOR. REGION ATOMICITY IS INTACT: `/tc accept N` keeps the whole region and
  `/tc reject N` removes it, both all-or-nothing, inline marks still forbidden inside.
- WITHDRAWN BEFORE DEPLOYMENT: 10.0.0's `/tc edits resolve`. It dissolved a region whose
  body the author had edited -- but `/tc accept N` on that same region already produces
  BYTE-IDENTICALLY the same result (wrapper gone, author's text kept, AI's text kept,
  paired gray removed). `accept` never inspected who wrote the body. What `resolve` bought
  was one commit per review session, and for that it inverted the 8.1.0 committed-content
  invariant and replaced git-native undo with a 3-generation snapshot store. Removed with
  it: decision='dissolve', _journal_baseline, snapshot.load_ai_baseline, the `pre-resolve`
  tagging, and nine tests. Net -184 lines of skill source.
  To refuse part of a region, DELETE it before accepting -- deletion is partial rejection.
- NEW, on `/tc accept`: a PROVENANCE CHECK for a `sourced`/`transcript` region. Content the
  region gained since the write gate saw it, with no basis in its gray excerpt, is NAMED
  before accept removes the now-false claim, and recorded in `.tc-history.md`. The
  comparand is the durable `supports:` audit record with NO git-HEAD fallback -- 8.1.0
  forces the author to commit their own edits first, so HEAD contains them and a fallback
  would compare their text against itself and always pass. With no record the answer is
  `not-checked`, never a silent pass. A CONSERVATIVE FLAG, NOT A PROOF: recombination,
  contradiction, deletion and quantifier swaps all measure zero, with human review as the
  documented backstop, exactly as 9.1.0's citation scanner.
- FIXED: a transcript region's durable evidence was WRITE-ONLY. `write_sourced_entry` logs
  it under a `transcript:` key (deliberately, so the citation manifest never lists an
  uncited excerpt) and nothing ever read that key back. `read_sourced_entries` gains an
  opt-in `include_transcript`; the manifest keeps its default, asserted.
- FIXED: `/tc accept` on a region the author EMPTIED left stray blank lines behind.
- FIXED (pre-existing): a resolved region's body never reached the git-committed audit --
  `audit.record` uses `extract_marks`, which is inline-only. Now logged as `was_body`.
- NEW: `/tc edits` reports the region-SCOPED diff -- what changed INSIDE a region, matched
  across the baseline by mark number -- not merely that a span overlapped one. Plus a
  warning that a region whose body is only a footnote definition renders as an empty div,
  so the "yellow is the visible contract" guarantee silently fails there.
- No new module, no new hook (count stays 6), back-compatible grammar, no document
  migration. 792-test suite (Category AI 9; 0 regressions).

9.9.3
2026-08-02
track-changes v9.9.3 -- /tc edits first-use frictions: the report now shows WHAT changed, and finds its own files.
- ORIGIN: the instructor's first live /tc edits run (2026-08-02), recorded as an 11-item friction
  log. Instructor's ordering: 9.9.3 before Phase B.
- FR-2 (the load-bearing one): the report located edits but did not SHOW them, so proofreading
  required re-reading the file and diffing it against the AI's own memory of what it had written.
  That works only in the session that authored the baseline -- a fresh, resumed, or compacted
  session had line numbers and nothing else, so the review step Phase A exists to support was not
  self-contained. compute_spans already built each span's before/after and discarded it; it is now
  returned and rendered. --no-diff restores counts-only; a long diff truncates LOUDLY (a silently
  shortened diff is worse than none -- the reviewer would believe they saw the whole edit).
- FR-1: bare `/tc edits` no longer errors. Deliberately NOT the working-file heuristic the
  resolution subcommands use -- that picks the most-recently-modified tracked file because no
  better state existed, and here it does: new snapshot.list_files() + current_sha256() +
  tc_edits.changed_files() compare shas, so the store names exactly which files the author changed
  and no snapshot content is read for unchanged ones.
- FR-3: the snapshot store had no read path (list + restore only), which is why FR-2 had no
  workaround. New `/tc edits diff` and `/tc edits show`, both read-only and asserted so.
- FR-6/7/10/11 (output honesty): the polish line states whether anything is PROPOSED instead of
  reporting a bare token count; site-scoped lint findings, which have no location BY CONSTRUCTION,
  are no longer framed as a configuration gap; a run that touches no region now says inline marks
  are unchecked (D8) rather than staying silent, since silence read as "no overlap"; and the footer
  states the rule for a defect in the AUTHOR's own text (unambiguous mechanical errors become
  normal marks, anything touching meaning is flagged and held).
- FR-4 NOT FIXED -- ITS DIAGNOSIS WAS DISPROVED. The log proposed exempting bare autolinks from the
  non-inline-construct test, while flagging that the regex was inferred and should be confirmed
  first. Confirming it disproved it: six shapes through the real hook all exit 0; the non-inline
  detector recognizes only fenced-code/display-math/yaml/gfm-table/quarto-div/latex and has NO HTML
  handling; cite.py's _MD_HTML_TAG sits on the sourced-citation path an authored mark never
  reaches; and "spans a non-inline construct" is a HINT appended to a block, never a block reason.
  The log conflated the hint with the cause, and the fix as written would have changed code that is
  not on the failing path. Recorded in the friction log with the evidence; still open pending the
  real payload.
- No new shipped module, so no manifest change (checked against the 9.9.2 rule, not assumed). No
  grammar, hook, protocol, or resolution change; hook count stays 6. 783-test suite (748 + 35 new
  Category AH; 0 regressions).

track-changes v9.9.2 -- verify the INSTALLED artifact: a deployed tree now checks itself, and both deployment paths refuse to report success over one that cannot load.
- MOTIVATION: 9.9.1 fixed the instance and added build-time guards, but the hole 9.9.0 exposed is a
  property of the PROCESS -- nothing in the PCV loop ever exercised the installed artifact. 9.9.0 was
  built, tested green at 721/721, PCV-recorded, and shipped a subcommand that raised ImportError on
  its first real invocation. Three gaps: install.sh reported "install complete" without checking that
  what it deployed could be loaded; the Pages bootstrap path (which never runs install.sh) had only an
  ENUMERATED list of file-existence reads, the same fragile pattern a third time; and no test drove
  the installed CLI's behavior, since presence is not loadability and loadability is not function.
- CONSTRAINT: detecting a MISSING module means knowing what should be there, and a user's machine has
  no source tree to compare against. So the expectation has to ship WITH the package, or install-time
  verification degrades into a third hand-written list with the same blind spot as the first two.
- CHANGE (self-declaration): tc_core.MODULES in lib/tc_core/__init__.py -- the package declares its
  own contents, beside the modules it names. TC-AG-5 fails if it drifts from the directory in either
  direction, so a list the suite enforces against its own subject is not the same thing as a list
  nobody checks.
- CHANGE (new shipped file): lib/tc_selfcheck.py runs two checks that do not subsume each other --
  every DECLARED module present and importable (catches a module that never shipped; an absent file
  leaves nothing to walk), and every DEPLOYED *.py across the three skills importable (catches a
  module that shipped but cannot load). sys.path/sys.modules restored per attempt so one failure
  cannot contaminate the next; deployed-but-undeclared drift reported too. Stdlib only; verified that
  no suite module imports a third-party package at module scope, so a failure is a real defect.
  Exit 0 / 1 (failures named) / 2 (not a track-changes tree).
- CHANGE (install gate): install.sh runs the self-check after copy_files and FAILS the install
  (exit 2) instead of printing "install complete" over a broken tree. An installer that verifies only
  what it was told to copy cannot catch its own omission, so it must check the RESULT.
- CHANGE (bootstrap gate): bootstrap.md Step 4 runs the self-check as the PRIMARY verification gate,
  replacing the enumerated steps 4/4a-4g, with the rationale stated inline. This is what gives a
  Pages install the same protection the maintainer's install.sh gets.
- TESTS: new Category AG drives the INSTALLED copies, never the source tree -- self-check passes on a
  fresh install; NEGATIVE CONTROL deleting a declared tc_core module (non-zero, names it, "NOT
  DEPLOYED", and independently flags the downstream tc_edits.py ImportError); install.sh refusing at
  the self-check gate specifically; the installed /tc edits end to end on a fixture (the invocation
  that crashed on 9.9.0); MODULES matching the directory; installed /tc list + /tc status.
- PROCESS: the dev CLAUDE.md now carries the standing rule -- verification means the INSTALLED
  artifact; a new shipped file is not done until it is in BOTH deployment manifests (and in
  tc_core.MODULES when it belongs to the package); run the feature from the installed tree before
  calling it verified; never hand-fix ~/.claude/skills to make something work.
- Additive: no mark-grammar change, no change to any existing resolution path, no new hook (stays 6),
  no document migration. 748-test suite (725 + 23 Category AG; 0 regressions).

track-changes v9.9.1 — packaging fix: 9.9.0's two new tc_core modules were listed in no deployment manifest, so an installed 9.9.0 could not run /tc edits at all.
- DEFECT: 9.9.0 added four shipped files. Its packaging pass updated the two skill-level entries
  (lib/tc_edits.py, reference/tc-edits.md) in both manifests and MISSED lib/tc_core/marknum.py and
  lib/tc_core/snapshot.py in all four enumerated lists (install.sh's REQUIRED_FILES + copy block;
  bootstrap.md's download list + verify steps). publish.sh was unaffected — it whole-tree syncs, so
  the modules reached Pages; the bootstrap just never downloaded them.
- IMPACT: (1) /tc edits was dead on arrival — tc_edits.py imports tc_core.marknum at module scope,
  so every invocation aborted with ImportError. (2) SILENT: post_tool_use.py imports tc_core.snapshot
  inside its deliberate best-effort try/except, so the hook exited 0 on every AI write and stored
  NOTHING — the baseline /tc edits diffs against never accumulated, and would have fallen back to
  git HEAD forever even after marknum was restored. tc-polish was unaffected (its marknum import has
  a documented local fallback for a pre-9.9.0 track-changes).
- FIX: both modules added to install.sh (REQUIRED_FILES + copy_one block) and bootstrap.md
  (download list + verify steps 4f/4g). install.sh's header source manifest, which had also drifted
  past tc_edits.py and tc-edits.md, corrected. bootstrap.md's version strings bumped from the stale
  9.6.0 (unchanged since 9.7.0 shipped) to 9.9.1 — at 9.6.0 the bootstrap would have refused to
  upgrade a 9.8.3 install and reported a 9.9.1 install as "newer than this bootstrap".
- REGRESSION TESTS (the general defect, not the instance): new TC-T-8 and TC-T-9 derive their
  expectations from the SOURCE TREE at run time rather than from a list — T8 installs into a scratch
  HOME and asserts every file under track-changes/{hooks,lib,reference,tools} reached the installed
  skill; T9 asserts the same set appears in bootstrap.md's download manifest. Excluded: the three
  maintainer-local 9.8.0 governance files a user install never receives. The exclusion list fails
  safe — a new unexcluded, uninstalled file fails the test loudly, so omitting a module now takes a
  deliberate act rather than an oversight. Verified as a NEGATIVE CONTROL: against the pre-fix
  manifests both tests FAIL, naming exactly the two missing modules.
- WHY 721 GREEN TESTS MISSED IT: the suite exercises the SOURCE tree, where both modules are present;
  nothing drove the INSTALLED tree's tc_edits.py. The one install-side check (T1) was an ENUMERATED
  spot-check of three tc_core files — the same failure mode as an enumerated copy list.
- Manifest and test only: no source file edited, no behavior, grammar, or resolution-path change,
  no new hook (stays 6), no document migration. 725-test suite (721 + 4; Category T 58 -> 62;
  0 regressions).

track-changes v9.9.0 — /tc edits Phase A: a new track-changes subcommand for the review loop where the AUTHOR edits the document instead of dictating corrections.
- MOTIVATION: the protocol already described this loop in prose ("hand-tweak, then AI-verified accept")
  but had no tooling for it. Observed live four times in one ISE 754 session (2026-08-01): the author
  resolved AI-marked content by editing the .qmd directly, and every instance cost a manual git diff, a
  manual proofread, a /draft round-trip, and hand-applied fixes. Six dictation-grade errors still
  reached the file that way, plus four convention slips caught only because the AI happened to hold the
  convention in memory. Two structural blockers: git cannot separate the author's edits from the AI's
  when both are uncommitted in one working tree (the normal review state), and conventions were checked
  at commit time, after the text was written and the moment of authorship had passed.
- CHANGE (new subcommand): /tc edits [file] — lives IN the track-changes skill
  (lib/tc_edits.py + reference/tc-edits.md), dispatched by /tc the way /tc source and
  /tc manifest are; no fourth skill directory and no new hook. It REPORTS and never edits
  the document, like
  /tc polish analyze. It diffs the file against a snapshot of "the file as the AI last left it" and
  reports the edited line spans (each classified content vs whitespace-only, so on-save formatter noise
  stays visibly separate), tc-polish's dictated scope over just those spans, the project linter's
  findings INSIDE them, any tracked region an edit overlaps, and the next free mark number. The model
  then emits corrections as ordinary marks through the always-on gate: the author's own text stays
  clean and only the AI's corrections are marked.
- CHANGE (baseline): new tc_core/snapshot.py, written by the existing PostToolUse hook BEFORE its
  /draft early return — that early return is exactly what makes the audit log blind to /draft writes,
  which is where most AI editing in this workflow happens. Per-user store (state/snapshots/, keyed by
  sha1 of the abspath, mirroring the .marks cache) rather than per-repo: no .gitignore entry needed in
  any consumer repo, and a verbatim content copy can never be committed by accident. Exact bytes, so a
  restore is byte-faithful; 3 generations per file, 4 MiB cap, 30-day opportunistic prune. Two rejected
  alternatives, recorded so they are not re-proposed: git HEAD (cannot separate the two authors) and
  reconstruction from .tc-history.md (blind to /draft). Cold start falls back to git HEAD and says so.
  The store doubles as an undo buffer: /tc edits snapshots | restore (diff shown; --yes required; the
  pre-restore bytes are saved first, so a restore is itself undoable).
- CHANGE (absorption guard): an empty diff means "no change since the last AI write", which is also
  what an AI write that ABSORBED the author's edits looks like. Warned on two independent signals — an
  uncommitted file, OR a differing previous snapshot generation (the signal that survives the file
  having been committed) — naming --gen 1 as the recovery.
- CHANGE (lint, D6): the convention command is PROJECT-supplied, configured in a repo-local
  .tc-edits.json (git root first, then the document's own directory). The skill stays generic — it is
  published for anyone and knows nothing about any project's rules. Argument list with NO shell (a
  string command is refused outright), argv printed before it runs, findings bucketed into in-span /
  elsewhere-in-file / other-files / unlocatable (listed, never dropped). Non-zero exit is normal; a
  missing executable, timeout, or bad config is loud and non-fatal. --no-lint / TC_EDITS_NO_LINT=1 opt
  out. Trust model documented in SKILL.md: this is executable configuration, like a repo's build
  scripts.
- CHANGE (shared): new tc_core/marknum.py is the single mark-number allocator (polish delegates, with
  its local copy retained only as a degraded-mode fallback and an anti-drift test asserting parity);
  polish_engine.analyze gains --baseline-file (a non-git baseline, mutually exclusive with
  --baseline-ref); tc_core.activation gains tc_should_track_inherent (activation ignoring the per-turn
  /draft suspension). Fixed along the way: the diff normalizes line endings on both sides, without
  which every line of a CRLF document read as edited.
- SCOPE: Phase A is CLEAN-PROSE scope. A region overlapping an edited span is reported, never resolved
  — region splitting, partial accept of an untouched remainder, provenance retyping, the inverted
  dirty-file invariant, and the heavy-edit threshold are all Phase B (a later 10.0.0, which is where
  region atomicity actually breaks). Additive: no mark-grammar change, no change to any existing
  resolution path, NO new hook (count stays 6 shipped + the maintainer-local governance hook), no
  document migration. Suite 721/721 (702 + 19 new Category AF), 0 regressions. BUILD AND TEST ONLY —
  install.sh/publish.sh deliberately not run against the real HOME or Pages in this increment.

9.8.3
2026-07-20
track-changes v9.8.3 — fix the /tc import clean-landing failure when the two PreToolUse hooks run in parallel.
- MOTIVATION: /tc import lands a converted block clean via a two-hook handshake — verified-import's
  PreToolUse hook (declared first) writes a one-shot sha-bound exempt sentinel over the proposed
  bytes, and the track-changes gate consumes it to skip the mark requirement. Claude Code runs the two
  Write|Edit|MultiEdit PreToolUse hooks IN PARALLEL, not in declared order, so the gate's consume
  routinely runs before verified-import's write lands the sentinel: no sentinel is found and the clean
  import is wrongly BLOCKED. Observed live in an ISE 754 session (tc.log: "verified import ... exempt
  sha 3636a9caec40" immediately followed by "BLOCK Edit ... 1 violations"). A sandbox confirmed the
  handshake works when the hooks run sequentially and fails only under parallel execution; the modules
  are correct (deterministic proposed bytes, round-tripping content_sha, LF file — no CRLF divergence).
- CHANGE: track-changes/hooks/pre_tool_use.py — the gate now ALSO honors a live pending-import RECORD
  as the clean-import signal. That record is written synchronously by /tc import staging BEFORE the
  write event, so it is race-free regardless of hook order. The gate allows the write clean when a live
  pending exists and does NOT clear it — verified-import remains the sole owner of the coverage gate,
  the audit entry, and the pending clear, so coverage/audit are never skipped. vi_verify is located
  sibling-relative (.../verified-import/lib), the way verified-import finds tc_core, so it resolves in
  both the deployed and source trees; best-effort import. The existing sentinel path is retained for
  the verified-import-first ordering.
- SCOPE: one guarded block in the track-changes PreToolUse gate + new hermetic test Category AE (3
  tests: live-pending allows, no-pending blocks, expired-pending blocks). verified-import's hook, the
  /tc source intra-hook sentinel, mark grammar, protocol, and CSS are unchanged. Hook count unchanged
  (6 shipped + maintainer-local governance hook). Suite 702/702 (699 + 3 AE), 0 regressions.

9.8.2
2026-07-18
track-changes v9.8.2 — document the markdown-note fallback for source-validation when an annotated highlight is not technically possible (docs).
- MOTIVATION: §16 documented WHEN the annotated-PDF twin cannot be built (image-only PDF,
  "no text layer") but not WHAT replaces it. Hit live in an ISE 754 session: the source
  backing the Holmstrom (1982) lecture endnote was an image-only JSTOR scan (no text layer,
  no OCR installed), so the highlight could not be snapped onto the verified excerpt. The
  claim was verified by reading the scanned pages and recorded as a hand-written markdown
  note in the topic's validation/ folder. Undocumented, that invites a silent omission (no
  evidence artifact for a scanned source) or an ad-hoc, non-portable response.
- CHANGE: SKILL.md §16 ("/tc manifest and the validation/ folder") gains a "Fallback when
  highlighting is not technically possible" paragraph. When a source has no searchable text
  (image-only scan, no text layer, no OCR), a hand-written markdown verification note in
  validation/ replaces the annotated twin, recording the claim, the verbatim excerpt(s)
  transcribed from the source, exact page references, the claim->support mapping, the
  verdict, and why the highlight was impossible. The same fallback covers the manual
  footnote-plus-evidence path (a settled paraphrase-with-citation kept as ordinary cited
  text, still verified); a fully highlight-compliant twin still needs a text-layer copy
  (save-then-source).
- SCOPE: documentation only. No code / hook / grammar / protocol / CSS / test change; the
  annotator's skip-image-only behavior is unchanged. Hook count unchanged (6 shipped +
  maintainer-local governance hook). No new tests (documentation).

9.8.1
2026-07-17
track-changes v9.8.1 — skip the Windows Store python alias stub in the interpreter probe.
- MOTIVATION: on Windows with the Microsoft Store "App Execution Alias" enabled but no Store
  Python installed, `python3` resolves to a reparse-point stub under
  .../Microsoft/WindowsApps/ that BLOCKS INDEFINITELY when executed and cannot be killed by a
  timeout. The shared Python-3 probe runs `python3` FIRST, so the UserPromptSubmit hook (and
  every other probe caller) hung until "UserPromptSubmit hook timed out after 30s". Raising the
  timeout does not help — the stub outlasts any timeout; it only delays the same failure.
- FIX: before executing a candidate, resolve its base binary with `command -v` (a path lookup,
  NO execution, so it cannot hang) and skip any candidate resolving under
  .../Microsoft/WindowsApps/. The loop then continues to the real interpreter (`python`/`py`).
  The pattern targets ONLY the per-user alias-stub dir: a python.org install, the `py` launcher,
  and even a real Store Python (under Program Files/WindowsApps, a different path) are unaffected;
  off Windows and on correctly-configured Windows the guard is a no-op.
- SCOPE: all 8 bash probe sites guarded — 6 shipped (lib/tc-common.sh, lib/tc-cli.sh x2,
  lib/tc-history.sh, hooks/pre-commit.sh, lib/migrate-v1-to-v2.sh) + 2 test-harness
  (tests/run-tests.sh preflight, tests/lib/helpers.sh tc_test_python) so the suite runs on an
  alias-affected machine. hooks/pre_tool_use.py needs no change (already runs under Python; no
  shell-probe). No protocol/grammar/hook-count/document change. New test Category AD (2 tests).

track-changes v9.8.0 — mechanically enforce "PCV for all changes" (maintainer tooling).
- MOTIVATION: 9.7.0 was edited, reinstalled, and PUBLISHED as a direct edit with no PCV
  artifacts and no critic pass, caught only retroactively. An instruction in CLAUDE.md is
  advisory; this adds mechanical enforcement at the chokepoints a change must pass to become
  real. All three mechanisms are DEVELOPER-LOCAL — end users install via the curl bootstrap
  and never run install.sh, so none of this ships or fires for them.
- A. DEPLOY GATE: lib/pcv_gate.py (new) checks a version's PCV record is complete (charge +
  plan + build-record with Critic & Verification sections + a decision-log
  [MILESTONE:VERIFICATION_COMPLETE] entry). install.sh (local install) and the new publish.sh
  (Pages) refuse to deploy an incomplete version; TC_FORCE_DEPLOY=1 overrides with a logged
  entry (pcvplans/logs/deploy-overrides.log). publish.sh also replaces the error-prone MANUAL
  Pages sync with a deterministic fixed-mapping sync (--dry-run previews) that EXCLUDES the
  maintainer-only files below, so they never ship.
- B+C. GOVERNANCE HOOK: hooks/pcv_dev_gate.py (new; PreToolUse) blocks edits to the INSTALLED
  copy (~/.claude/skills/{track-changes,verified-import,tc-polish}/**) and to skill SOURCE
  (v*/...) without an in-flight charge (a charge-<next>.md > VERSION, or pcvplans/.pcv-active).
  Installed MAINTAINER-ONLY via install.sh --dev-guards (registered FIRST in settings.json by
  lib/dev_guards_register.py); NEVER in the public bootstrap. Path-matching is normalized
  (expanduser+realpath+normcase), component-exact + pcvplans-sibling (no false hits on a folder
  merely containing "track-changes"), with a fast token pre-filter (no disk touch for unrelated
  paths) and fail-OPEN on error (never wedges editing).
- The 6 MARK/import hooks and the mark protocol are UNTOUCHED; the governance hook is a separate,
  maintainer-local 7th PreToolUse hook (not part of the shipped 6). No shipped-skill content
  change (published files identical; only version.txt/index footer bump). New tests: Category AC
  (3). Full suite 696/696. Built THROUGH its own gate (dogfood).

9.7.0
2026-07-14
track-changes v9.7.0 — restore the AI-driven transcript gray+gloss pairing (regression fix).
- REGRESSION FIXED: the class-recording TRANSCRIPT workflow (8.1.0) — the AI finds the
  matching span of the instructor's recording, drops it in as a gray `.tc-verbatim` block,
  and writes a green `tc-prov="transcript"` gloss (polished wording) so the instructor can
  confirm the gloss preserves the meaning — was silently blocked by 9.1.0's "ironclad
  sourced" gate, which wired EVERY new gray block to require a `sourced` region carrying a
  reader-facing citation. A gray excerpt now pairs with EITHER a `sourced` region (must
  cite) OR a `transcript` region (NO citation — it is the lecture's own voice, not an
  external source). Verification is unchanged for both: the gray excerpt is still re-read
  from the staged source and refused fail-closed if fabricated/paraphrased (containment),
  and `tc-src`/`tc-n` are still enforced. Only the citation requirement is lifted for
  `transcript`. The accept-side already removed gray scaffolding for sourced/`transcript`
  regions alike (9.3.0), so the write gate was the sole out-of-step piece.
- STAGING: `/tc source <transcript>#L<a>-L<b> <target>` now offers both green shapes — (a)
  `sourced` (document source, cited) and (b) `transcript` (class narration, uncited) — and
  the "must carry a citation" rule is stated as sourced-only.
- AUDIT: a verified transcript pair is logged under a `transcript:` key (not `sourced:`) so
  the citations manifest never lists an uncited transcript excerpt; evidence (verbatim
  excerpt + supported gloss) is recorded identically.
- Hook + tc_source + audit change only; grammar unchanged (`transcript` was already a
  PROV_VALUE since 8.1.0); no new hooks (suite stays 6); no document migration. New tests
  Z39–Z41 (transcript lands uncited; fabricated excerpt still refused; sourced still
  requires a citation). Full suite 691/691.

9.6.0
2026-07-14
track-changes v9.6.0 — tc-polish cumulative baseline + region-aware numbering.
- CUMULATIVE BASELINE (feature): `/tc polish` (engine `analyze`) now AUTO-resolves its
  git baseline to *cumulative edits since the last polish*, so commit-as-you-go workflows
  (every edit checkpointed, e.g. a `done.py` writing `Instructor edits: <file>`, leaving
  HEAD always current) no longer see an empty scope. Precedence: explicit `--baseline-ref`
  → per-file pinned checkpoint → AUTO (newest commit whose subject matches the resolution
  pattern, default `^Accept polish marks` — the last polish point, so each accepted run
  resets the window) → FALLBACK (the commit just before the current edit-streak, default
  `^Instructor edits: <basename>`) → HEAD. The reset anchor is the RESOLUTION commit, not
  the polish call. Patterns are CONFIGURABLE (repo-tracked `.polish-baselines.json` `config`
  block) so the feature is generic, not course-specific; the diff stays baseline→on-disk so
  uncommitted edits are in scope; prior marks/regions are still reduced before tokenizing.
  New engine CLI surface: `polish-cli.sh resolve|set|clear|show <file>`.
- REGION-AWARE next_mark_n (bug fix): the polish next-mark number now counts track-changes'
  WHOLE-REGION numbers (md `tc-n="N"`, LaTeX `\begin{tcregion}{N}`) in addition to inline
  marks, so a polish mark can no longer collide with a pending `.tc-region` (observed:
  next=1 while §3 held regions 14–29). Returns max(all)+1, or 1.
- tc-polish skill only; no change to the track-changes hook, mark grammar, or `/tc accept`
  mechanics; no new hooks (suite stays 6); no document migration. New test category AB.

9.5.1
2026-07-13
track-changes v9.5.1 — bare-class .tc-verbatim recognition (matcher bug fix).
- The 9.3.0 gray-pairing matcher (`grammar.extract_verbatim_blocks`) recognized the
  brace-class form `::: {.tc-verbatim …}` but not the bare-class form `::: tc-verbatim`
  — both valid Pandoc for a div of class `tc-verbatim`. Sourced excerpts carry
  attributes (brace form); attribute-free transcript "Your words" blocks are authored
  bare — so on `/tc accept`/`reject` a bare gray block was left behind, needing a manual
  deletion (the friction 9.3.0 removed). Now recognized in EITHER form at any fence
  length (`:::`, `::::`, …); matched precisely (whole attribute token == the class), so a
  class name appearing as a brace-block VALUE is never mis-detected.
- Matcher-only: the adjacency pairing rule, the `gray_removed` audit field, and the
  authored/non-adjacent guarantee are unchanged. The region matcher stays dot-only (a
  numbered region needs `tc-n`, which forces the brace form, so a bare `::: tc-region` is
  never a usable region). No document migration; no new hooks (6); back-compatible.

9.5.0
2026-07-13
track-changes v9.5.0 — web-capture robustness (additive; JS pages, thin/empty snapshots, fetch errors).
- Improves the 9.2.0 `/tc source <url>` capture on three reliability axes; no change to
  the SSRF gate, the mark grammar, the hook, or audit/manifest.
- JS-rendered pages: the headless Chrome render now passes `--virtual-time-budget`
  (+ `--run-all-compositor-stages-before-draw`) so async (JS/XHR-built) content settles
  before `--print-to-pdf` fires — many pages that previously captured as an empty
  pre-hydration shell now render their real content (verified end-to-end against a
  JS-injecting fixture).
- Thin/empty snapshots: a FULLY empty page still fails closed, now with a message that
  names the JS-page likelihood and the save-then-source workaround; a NEAR-empty
  snapshot (normalized text below ~120 chars — the shell signature) still stages but
  emits a LOUD advisory naming the char count + workaround, so a genuinely short page
  (e.g. example.com) is never blocked while a silent shell no longer stages unnoticed.
- No junk left behind: a fail-closed capture removes any partial snapshot it wrote, and
  an unusable browser-rendered PDF is discarded before the HTML fallback.
- Fetch errors: an SSL/certificate failure in the validated fetch now reports plainly
  (naming the cert problem) with the save-then-source workaround instead of a bare
  exception.
- Additive/robustness only; no new hooks (suite stays 6); no document migration.

9.4.0
2026-07-13
track-changes v9.4.0 — region acceptance: paragraph continuity (additive, opt-in).
- PROBLEM: a region carved from inside a paragraph (a mid/end sentence pulled out to
  source or mark it) sits as its own block; on `/tc accept` the wrapper is stripped but
  the block's blank-line boundaries remain, leaving the sentence as a standalone
  paragraph instead of rejoining the one it came from — right text, wrong paragraphing,
  silently.
- `tc-join` (opt-in, the proper fix): a region may declare that it rejoins an adjacent
  paragraph on accept — md/qmd `::: {.tc-region … tc-join="prev"}` (merge into the
  PRECEDING paragraph) or `tc-join="next"`; LaTeX a 4th optional on `tcregion`,
  `\begin{tcregion}{N}[prov][src][join]` (arg spec `m o o o`, back-compatible — the
  macro accepts and render-ignores it). On accept the body is spliced onto the end
  (`prev`) / start (`next`) of the neighbouring paragraph with a single space. The
  neighbour scan skips gray `.tc-verbatim` scaffolding (so a sourced region rejoins the
  paragraph ABOVE its excerpt block, and a `prev` merge subsumes that gray block).
  Absent the attribute, resolution is byte-for-byte as in 9.3.0. No valid neighbour
  (BOF/EOF or a structural neighbour) ⇒ fall back to the standalone strip + a note.
- Accept-time orphan warning (always-on safety net): after `/tc accept`, an advisory
  WARNING (never a block; exit unchanged) fires when a resolved region left a
  single-line paragraph sandwiched between two body paragraphs — the region-split
  signature — suggesting `tc-join`. A joined region, or one resolved at a real
  structural boundary, does not trigger it.
- Additive audit field `join: prev|next` on joined regions. Back-compatible grammar; no
  new hooks (suite stays 6); no document migration.

9.3.0
2026-07-13
track-changes v9.3.0 — sourced-region lifecycle: gray cleanup + auto-evidence (behavior-changing).
- GRAY CLEANUP: `/tc accept` (and `/tc reject`) of a `sourced`/`transcript` region now also removes
  its paired gray `.tc-verbatim` scaffolding block in the same resolution. The block's sole purpose
  (side-by-side confirmation) is discharged at resolution and its durable record persists in the
  `sourced:` audit entry, so nothing is lost. Closes the self-contradiction where SKILL.md prescribed
  deleting the gray block but the deletion gate refused the AI's unmarked deletion. Pairing is
  conservative — the gray block immediately above the region (blank lines only between); a region with
  no adjacent block, or an authored/imported region, is untouched. Runs in the resolver's own direct
  write, so the always-on hook is never involved. Markdown/Quarto (`.tc-verbatim`) and LaTeX
  (`tcverbatim`) parity.
- AUTO-EVIDENCE: the human-facing validation artifacts are now generated automatically instead of
  on-demand. (1) MANIFEST: a successful write that touches a `sourced` region refreshes
  `validation/<stem>.sources.md` from the just-recorded audit (in the existing PostToolUse hook;
  gated so ordinary tracked writes never regenerate it). (2) ANNOTATED TWIN: `/tc accept` of a
  sourced region regenerates the highlighted annotated-PDF twin(s) for the touched source(s). Both
  are BEST-EFFORT and LOUD on failure — a missing browser / PyMuPDF / unreadable source reports a
  note but NEVER fails the write or the accept (excerpt *verification* is unchanged and still fails
  closed). New importable `tc_manifest.regenerate(doc)`; the annotator runs as an isolated subprocess.
- CSS: the provenance `::before` label no longer overlaps the first body line — `.tc-region` /
  `.tc-verbatim` top padding bumped to clear the absolutely-positioned label.
- Additive audit field `gray_removed: true` on resolved sourced/transcript regions (back-compat).
  No new hooks (suite stays at 6); backward-compatible grammar; no document migration.

9.2.0
2026-07-13
track-changes v9.2.0 — web-source citation with a captured dated snapshot (additive).
- `/tc source <url>` (or `/tc source @webkey`, a bib `@online`/`@misc` entry with `url`+`urldate`)
  captures the page AT STAGE TIME into a dated snapshot under the document's private
  `validation/sources/` folder, extracts its text, and stages a pending-source record pointing at
  the LOCAL snapshot. From there everything reuses v9: the always-on hook verifies the gray excerpt
  against the snapshot text (normalized-exact containment, NETWORK-FREE), the 9.1.x citation
  requirement + anchor apply, and the `sourced:` audit / `/tc manifest` record the URL + access date
  + snapshot. The snapshot is the durable, viewable proof that gives the access date teeth against
  link rot.
- Capture: a discovered headless Chrome/Chromium/Edge renders a `.pdf` (`--print-to-pdf`,
  discovered PATH-first then per-OS install globs); no browser / render failure / timeout falls back
  LOUDLY to a `urllib` fetch → `.html` text snapshot. Both paths extract text the hook can verify.
- Security: capture is SSRF-guarded — `validate_url` accepts only public `http(s)` URLs and rejects
  loopback / private / link-local / cloud-metadata (169.254.169.254) / reserved addresses before any
  fetch (a `TC_SOURCE_ALLOW_LOCAL=1` test opt-in permits loopback only). The browser is invoked as an
  ARGUMENT LIST with `shell=False` (the URL is one argv element) — never a shell string.
- Snapshots stay in the private `validation/` folder (never a public repo). Authenticated /
  paywalled pages are a NON-GOAL: save the page to a file and `/tc source` the file path.
- New shared lib `tc_core/websource.py` + a `.html`/`.htm` branch in `tc_core/sourcetext.py`;
  `srcstage`/`tc_source`/audit/manifest carry the URL + access date + snapshot. Backward-compatible
  (a file/PDF/docx source is unchanged); no new hooks (suite stays at 6); no document migration.

9.1.1
2026-07-12
track-changes v9.1.1 — contextual gray excerpts with a marked source anchor (additive).
- A gray `.tc-verbatim` excerpt may now carry a CONTIGUOUS passage of surrounding source context
  (so a reviewer can place the attribution without reopening the source), with the load-bearing
  sentence — the part actually proposed as the source — wrapped in a source-anchor marker:
  md/qmd `[sentence]{.tc-src-key}`, LaTeX `\tcsrckey{sentence}`. The whole block stays gray (all of
  it is verbatim source); the anchor is emphasized (bold + underline, no new color).
- The write-time containment check STRIPS the anchor marker syntax before verifying, so an anchored
  excerpt still verifies verbatim against the plain source; only the syntax is removed — the inner
  text must still be contained, so an anchor cannot smuggle non-source text. The durable audit /
  manifest excerpt records the clean (anchor-stripped) text.
- Guidance (no gate change): the `/tc source` instruction now asks for a contiguous+anchored excerpt
  and reminds that a sourced region must stay SCOPED to what the source supports — an inference built
  on top of the source is authored (uncited) content, not sourced, so a single citation never creeps
  over unsupported claims.
- Purely presentational + guidance; no new hooks (stays 6); backward-compatible (excerpts without an
  anchor are unchanged); no new shipped files; no document migration.

9.1.0
2026-07-12
track-changes v9.1.0 — ironclad sourced-region enforcement (behavior-changing).
- `tc-prov="sourced"` is now an ALWAYS-VERIFIED, ALWAYS-CITED claim, enforced at the write so
  it holds against an uncooperative/forgetful AI agent (not by prompt discipline):
  - The gate now triggers on a new gray `.tc-verbatim` excerpt OR a touched sourced region. A
    sourced region can no longer bypass verification by omitting the gray excerpt (the v9.0
    hole): a brand-new sourced region and its verified gray excerpt must land as ONE pair.
  - The surviving green region MUST carry a reader-facing citation (tc-src is invisible
    metadata, stripped on `/tc accept`). Rule A: staged by `@citekey` → the region must cite
    THAT key (`[@key]` md / `\cite{key}` tex). Rule B: otherwise → any citation/footnote token
    (`[@key]`/`@key`/`^[...]` md; `\cite`-family/`\footnote` tex). Citations buried in code
    spans, HTML/LaTeX comments, or verbatim/`\verb` do NOT count (they render nothing).
  - Editing an already-present sourced region needs no re-staging but must keep a citation — a
    confirmed sourced passage cannot be edited into an unattributed one.
- New shared lib `tc_core/cite.py` (data-only citation detector). `/tc source` instruction now
  tells the agent to include the citation (the exact key when staged by key).
- Additive/back-compat grammar; no new hooks (stays 6); no document migration. Adversarially
  red-teamed (a comment-buried-citation bypass was found and closed).

9.0.0
2026-07-12
track-changes v9.0.0 — source-validation discipline, suite-wide.
- SOURCE-VALIDATION DISCIPLINE: AI text supported by a document source lands as a green
  `sourced` region beside a temporary gray `.tc-verbatim` excerpt that is verbatim BY
  CONSTRUCTION — `/tc source` stages the slice and the PreToolUse hook re-reads the source at
  write time, refusing a fabricated or paraphrased excerpt (exit 2). Containment is
  normalized-exact (NFKC fold, soft-hyphen/ligature/line-break-join, whitespace collapse); a
  scanned/unreadable source fails closed. Sources: text (`.md`/`.qmd`/`.tex`/`.txt`), `.pdf`
  (PyMuPDF), `.docx` (python-docx).
- PROVENANCE + tc-src: `tc-prov="sourced"` joins authored/imported/transcript; a sourced
  region carries `tc-src` binding it to `path#locator` or an `@citekey locator` resolved via
  the document's `.bib` `file`/`localfile` field or a `.tc-sources.json` map (unresolvable
  keys fail closed, naming both mechanisms).
- /tc manifest [<doc>]: regenerates `validation/<stem>.sources.md` whole-file and
  deterministically from the `sourced:` audit entries in `.tc-history.md` — one section per
  region (source, locator, verbatim excerpt, supported text, links both ways) plus a
  "Resolved/removed regions" list. Machine-generated evidence, never model-reconstructed.
- tools/annotate_source_pdf.py: highlights each verified excerpt in an annotated PDF TWIN
  (never the original) under `validation/`, regenerates a trailing summary page, and reports
  every unmatched excerpt (nonzero exit); image-only PDFs are reported and skipped.
- LaTeX first-class: `tcregion` gains an optional trailing src arg (`{N}[prov][src]`, xparse
  `m o o` — absent second optional leaves every v6/v7 parse untouched) plus a `tcverbatim`
  gray-scaffolding environment and green transcript/sourced change-bars. Suite-shipped
  `tc-clean.css` styles all four provenances + `.tc-verbatim` under both raw and `data-`
  attribute forms (a course `_quarto.yml` can shrink to a thin consumer).
- Additive and backward-compatible: mark grammar unchanged (absent provenance/src = authored);
  no new hooks (suite stays at 6); no document migration.

8.2.0
2026-07-12
track-changes v8.2.0 — import-fidelity coverage check for verified-import.
- COVERAGE GATE: a verified import can no longer silently drop source content (the recurring
  "translated only part of a slide" failure, e.g. dropping r_f of four rates). New shared lib
  tc_core/coverage.py compares CONTENT TOKENS (words >=4 letters minus stoplist, numbers >=2
  significant chars, subscripted ids like r_f) with formatting normalized away — reformatting
  passes, dropped content blocks (hook exit 2, missing tokens named, pending import preserved
  for a corrected retry). Fail-closed when the source cannot be re-read at write time.
- OVERRIDE: /tc import --allow-partial stores allow_partial on the pending record; the hook
  then lands the write and records the override + dropped list in the audit entry.
- AUDIT: new /tc coverage <doc> <source> [--units N,N,...] reports per-unit % covered and
  missing tokens (--slides accepted as alias); exit non-zero on any drop.
- Additive and backward-compatible: optional pending-record key; import behavior otherwise
  unchanged; no new hooks (suite stays at 6); no document migration.

8.1.0
2026-07-12
track-changes v8.1.0 — committed-content invariant + transcript provenance.
- RESOLUTION GATE: accept/reject/accept-all/reject-all now REFUSE (exit 3) if the target
  file has uncommitted changes or is untracked (tc_require_clean in lib/tc-cli.sh). An open
  mark is editable right up to resolution, so approval could otherwise attach to content the
  reviewer never read (observed live: an AI polish correction applied and accepted in one
  step). Enforced sequence: commit instructor tweaks (own commit) -> commit AI corrections
  as MARKED edits (own commit) -> review diffs -> resolve. list is not gated; TC_FORCE=1 is
  a human-only override; fail-open outside a git repo. No new hooks; suite stays at 6.
- PROVENANCE (v7 work, first published here): tc-prov="transcript" joins authored/imported
  in tc_core/grammar.py PROV_VALUES — AI wording over the instructor's own class-recording
  transcript content; conventionally paired with a TEMPORARY .tc-verbatim scaffolding block
  (delete after confirming). Documented in SKILL.md (Provenance, Fix E extended) + digest.
- Mark grammar note (from the same incident): the <s>OLD</s> form cannot re-present an
  already-applied change for approval (OLD must still exist in the file) — the rule is that
  polish corrections are marked BEFORE resolution, never applied during it.

8.0.0
2026-06-27
track-changes v8.0.0 — bundle decap, a deterministic author-invoked dictation pre-clean filter (stdin→stdout), shipped with the suite under track-changes/tools/ but outside the mark protocol; no new hook, no mark-grammar change, suite stays at 6 hooks.
